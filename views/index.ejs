<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <%= 2 * 2 %> <%= Math.random() %>
    
    <p> <%= gender %></p>

    <% if (gender == "male") { %>
        <p><%= name %></p>
    <% }else{ %>
        <p>Na woman you be</p>
    <%}%>
</body>
</html>